import os
os.environ.setdefault('DJANGO_SETTINGS_MODULE','rate_my_ride.settings')

import django
django.setup()
from ride.models import Category, Page

def populate():

    glasgow_taxi_pages = [
        {'title'}
    ]